import { rerenderCategories } from './rerenderCategories';
import { rerenderNotes } from './rerenderNotes';
import { rerenderStats } from './rerenderStats';

export const rerender = {
  categories: rerenderCategories,
  notes: rerenderNotes,
  stats: rerenderStats,
};
